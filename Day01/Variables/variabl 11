#include <stdio.h>

int main() {
    float su , lo , la ;
    printf("entre la largeur : ");
    scanf("%f",&la);
    printf("entre la longueur : ");
    scanf("%f",&lo);
    su = lo * la ;
    printf("surface = %f",su);

    return 0;
}

Envoyer un message à @mohammed
